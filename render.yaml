services:
  - type: web
    name: profile-outreach-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: EXA_API_KEY
        sync: false

